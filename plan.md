# rish-mcp

---
## 만드는 목적

---
> **기존의 AI는 사용자의 기기를 조작하지 못하고 개발에 가상머신을 사용해 실기기태스트가 불가능함**  
> **사용자의 기기에 문제가 발생할때 기존에는 `adb...`명령어를 치라고만 할수있었음**
  
## 처음부터 다시 시작하는 이유

---
> **기존코드는 너무 난해함**  
> **WearOS같은 저사양 기기에서는 퍼포먼스가 떨어짐**  
> **shizuku 의존성이 있어 shizuku를 모르거나 지원이 안돼는 기기는 사용이 불가함**  
> **서버측이 퍼포먼스가 부족함**  
> **전반적으로 코드질이 쓰래기임**
> **버전 정보나 그런걸 담은 공식서버의 부제**

---
## 목표 (재구성이 반드시 만족해야 하는 것)

---
> **온보딩 장벽 제거** — Shizuku 앱 설치/실행 없이 동작해야 함 (앱이 ADB로 직접 페어링해서 셸 권한 획득)  
> **저사양 기기 대응** — WearOS 등에서 배터리/메모리/CPU 소모가 눈에 띄게 낮아야 함 (상시 연결 유지 비용 포함)  
> **서버 성능** — relay/MCP 서버가 명령 처리 지연과 동시 접속 처리에서 기존보다 확실히 개선되어야 함  
> **코드 품질** — 테스트, 타입 안정성, 레이어 분리가 명확해서 유지보수 가능한 수준이어야 함  
> **공식 서버 존재** — 버전 정보(최신 APK 버전 등)를 조회할 수 있는 공식 호스팅 엔드포인트를 유지해야 함

---
## 비목표

---
> **root 권한을 요구하지 않음** — 기존과 마찬가지로 shell(uid 2000) 수준 이하로 동작  
> **multi-tenant 서비스가 아님** — 개인 소유 기기를 스스로 제어하기 위한 용도로 한정  
> **완전 무탭(headless) 설치는 더 이상 목표가 아님** — 최초 1회는 기기 화면에서 수동 개입(페어링 코드 확인 또는 USB 승인)이 필요함을 허용

---
## 아키텍처 결정: 셸 접근 방식 (Shizuku 대체)

---
> **Shizuku 앱 의존성 완전 제거.** rish-mcp 앱이 스스로 ADB 프로토콜로 자기 자신에게 연결해서 셸(uid 2000) 권한을 얻음  
> **Android 11 이상** — 설정 > 개발자 옵션 > 무선 디버깅의 페어링 코드를 최초 1회 앱에 확인/입력 → 이후 앱이 자동으로 재연결·재프로비저닝  
> **Android 11 미만** — 무선 페어링 API 자체가 없으므로, 최초 1회 USB 케이블로 연결해 USB 디버깅을 승인하는 방식으로 대체  
> **트레이드오프 수용** — 기존 Shizuku 방식의 "무탭 헤드리스 설치" 강점은 포기. 최초 1회 수동 개입은 허용하고, 그 이후 흐름(재연결, 재프로비저닝, OTA 업데이트 등)은 계속 자동화 유지

---
## 아키텍처 결정: 연결 모델 & 저사양 기기 성능 목표

---
> **기기별 하이브리드 연결 모델** — 일반 폰/태블릿은 기존처럼 상시 WebSocket 연결 유지(응답성 우선), WearOS 같은 저사양 기기는 온디맨드/주기적 연결로 전환(배터리 우선)  
> **WearOS 등 저사양 기기 유휴 배터리 소모 목표: 시간당 2~3% 이내** — 기존 Shizuku 방식(핑 25s/60s, 하트비트 30s/90s)보다 눈에 띄게 개선된 수준. 상시 연결을 유지하는 일반 기기와 달리, 저사양 기기는 이 목표를 맞추기 위해 온디맨드/주기적 연결 쪽으로 최적화

---
## 아키텍처 결정: 서버 스택

---
> **Node/TS → Go로 전면 재작성.** relay + MCP 서버, OAuth 레이어까지 모두 새로 구현  
> **이유** — 동시 연결 처리와 메모리 효율을 최대화하고, 단일 바이너리 배포로 컨테이너를 가볍게 유지하기 위함. goroutine/channel 기반 동시성 모델이 WS relay 구현에 직관적으로 맞음  
> **수용하는 리스크** — MCP Go SDK는 비공식/미성숙이라 MCP 프로토콜·OAuth·relay 프로토콜을 자체 구현해야 함. 개인 프로젝트 규모 대비 과한 투자일 수 있음을 인지한 상태로 진행

---
## 아키텍처 결정: 공식 서버 범위

---
> **버전 정보 + APK 배포만 담당** — 기존 `PUBLIC_MCP_HOST`와 동일한 역할: 최신 버전/체크섬 조회, APK 다운로드만 제공. 토큰이나 기기 정보 같은 민감 데이터는 다루지 않음  
> **relay는 공식 호스팅하지 않음** — 사용자가 각자 relay를 셀프호스팅. "multi-tenant 서비스가 아님" 비목표와 일관되게, 공식 서버에 자기 기기를 붙이는 옵션은 제공하지 않음

---
## 아키텍처 결정: Android 11 미만 USB 한계

---
> **PC + adb가 실제로 필요함** — 단순 충전 케이블 연결이 아니라, 최초 1회 PC에서 `adb tcpip`로 전환 명령을 날려줘야 무선 브리지가 생김  
> **재부팅 시 풀릴 수 있는 한계를 수용** — ROM에 따라 재부팅 후 설정이 유지되지 않으면 매번 USB 재연결이 필요할 수 있음. 별도 우회는 시도하지 않고, pre-11 기기의 알려진 한계로 문서화만 하고 진행

---
## 아키텍처 결정: 저사양 기기 웨이크업 방식

---
> **FCM 푸시로 깨우기** — 명령이 오면 relay가 FCM으로 기기를 깨우고, 기기는 짧게 WS 연결해서 실행·결과 반환 후 바로 종료. Wear OS 3+는 대부분 GMS를 탑재하므로 적용 가능  
> **주기적 폴백 유지** — FCM 전달 실패에 대비해 주기적 폴링을 폴백으로 유지 (구체적 주기는 구현 시 확정)

---
## 아키텍처 결정: 유휴 메모리·CPU 목표치

---
> **유휴 메모리 <50MB** — 배터리 목표(시간당 2~3%)와 일관된 현실적 수준  
> **CPU는 웨이크업/폴링 순간에만 짧게 사용** — 그 외에는 완전 유휴 상태 유지

---
## 다음 단계

---
> 위 결정 사항들로 목표·비목표·핵심 아키텍처(셸 접근, 연결 모델, 서버 스택, 공식 서버 범위)가 모두 정리됨. 이후는 구체적인 설계/구현 단계로 넘어갈 수 있음
