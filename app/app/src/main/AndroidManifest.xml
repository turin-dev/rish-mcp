<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- One APK for normal Android devices and Wear OS watches. -->
    <uses-feature
        android:name="android.hardware.type.watch"
        android:required="false" />

    <!-- ADB wireless-debugging pairing/connect (loopback) and the relay
         WebSocket both need network access. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
      MainActivity (pairing + provisioning UI) doesn't exist yet, so there's
      still no way to flip Prefs.enabled/relayUrl/deviceToken/adbHost/adbPort
      from the device's screen. AgentService/BootReceiver/ConnectionManager
      are otherwise fully wired (docs/DESIGN.md §2.1).
    -->
    <application android:allowBackup="false">

        <service
            android:name=".AgentService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Maintains an outbound control channel for owner-authorized remote shell automation on this device." />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
